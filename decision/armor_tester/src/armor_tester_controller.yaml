armor_tester_controller:
  unitree_joint:
    name:
      {
        type: string,
        default_value: "unitree_motor",
        description: "specify the name of the unitree motor",
      }
  dji_joint:
    name:
      {
        type: string,
        default_value: "dji_motor",
        description: "specify the name of the dji motor",
      }
